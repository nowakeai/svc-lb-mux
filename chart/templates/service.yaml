apiVersion: v1
kind: Service
metadata:
  name: {{ include "svc-lb-mux.fullname" . }}
  labels:
    {{- include "svc-lb-mux.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
    - port: {{ .Values.service.healthPort }}
      targetPort: health
      protocol: TCP
      name: health
  selector:
    {{- include "svc-lb-mux.selectorLabels" . | nindent 4 }}
