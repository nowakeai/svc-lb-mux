apiVersion: v1
kind: Service
metadata:
  name: {{ include "lb4-multiplexer.fullname" . }}
  labels:
    {{- include "lb4-multiplexer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
    - port: {{ .Values.service.healthPort }}
      targetPort: health
      protocol: TCP
      name: health
  selector:
    {{- include "lb4-multiplexer.selectorLabels" . | nindent 4 }}
