{{- if .Values.rbac.create }}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "svc-lb-mux.fullname" . }}-rolebinding
  labels:
    {{- include "svc-lb-mux.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "svc-lb-mux.fullname" . }}-role
subjects:
- kind: ServiceAccount
  name: {{ include "svc-lb-mux.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
{{- end }}
