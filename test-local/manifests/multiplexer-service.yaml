apiVersion: v1
kind: Service
metadata:
  name: mux
  namespace: svc-mux
  annotations:
    svc-mux.nowake.ai/multiplexer: "true"
spec:
  type: LoadBalancer
  ports:
    - name: placeholder
      protocol: TCP
      port: 8888
      targetPort: 8888
status:
  loadBalancer:
    ingress:
      - hostname: multiplexer.local
# ---
# # endpoint slice
# apiVersion: discovery.k8s.io/v1
# kind: EndpointSlice
# metadata:
#   name: mux
#   namespace: svc-mux
#   labels:
#     kubernetes.io/service-name: mux
# addressType: IPv4
# endpoints:
#   - addresses:
#       - 172.19.0.2
#     conditions:
#       ready: true
# ports:
#   - name: https
#     port: 8888
#     protocol: TCP
